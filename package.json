{
  "name": "agrisaathi",
  "version": "0.1.0",
  "private": true,
  "description": "Agricultural decision-support platform with live weather, persisted farm data, and reproducible ML pipelines.",
  "type": "commonjs",
  "engines": {
    "node": ">=24.0.0",
    "python": ">=3.10"
  },
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "test": "node --test tests/api.test.js",
    "lint": "node --check backend/server.js && node --check backend/config.js && node --check backend/db.js && node --check frontend/app.js",
    "ml:crop:download": "python ml/scripts/download_crop_dataset.py",
    "ml:crop:prepare": "python ml/scripts/prepare_crop_dataset.py",
    "ml:crop:train": "python ml/training/train_crop_model.py",
    "ml:disease:validate": "python ml/scripts/validate_plantvillage.py"
  }
}
